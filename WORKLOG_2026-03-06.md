# Worklog - 2026-03-06

## Что сделали сегодня

1. Разделили окружения на Demo и Product Dev стенды.
2. Добавили устойчивые скрипты запуска UI:
   - `scripts/start_ui_persistent.ps1`
   - `scripts/stop_ui_persistent.ps1`
   - `scripts/status_ui_persistent.ps1`
3. Обновили стартовые скрипты стендов:
   - `scripts/start_investor_demo.ps1`
   - `scripts/start_prod_ready_dev.ps1`
4. Перевели запуск UI на локальный код репозитория через `scripts/run_ui_server.py` (без зависимости от потенциально устаревшего `pulsar ui` entrypoint).
5. Исправили проблему `ERR_CONNECTION_REFUSED` / `Not Found` для `/experiments`:
   - проверка health и маршрута `/experiments` при старте;
   - единые порты и диагностика через `status` скрипт.
6. Добавили синхронизацию и устойчивость статусов CLI-экспериментов:
   - heartbeat при старте CLI run;
   - автоматический перевод "зависших" `running` экспериментов в `failed` при отсутствии прогресса > 90 минут.
7. Подтвердили в UI корректный переход зависшего DPO run в `failed` и восстановили стабильный доступ к `http://127.0.0.1:18088/experiments`.

## Ключевые причины инцидентов

- В браузере открывались порты, где сервер не был запущен.
- Часть запусков шла через другой runtime/entrypoint, из-за чего UI и API работали несогласованно.
- Один из DPO запусков фактически остановился на ранней стадии, но статус в UI оставался `running`.

## Что осталось на следующий шаг

1. Перезапустить DPO run в контролируемом фоне и мониторить `outputs/demo-logs/*.log`.
2. Добавить отображение причины auto-fail в карточке эксперимента (UI).
3. Добавить отдельную команду/кнопку "reconcile stale runs" в интерфейс/CLI.
